program.main => "test.adn "
{
    Output > "coucou"

    if 1 == 1 {
        Output > "hello !"
    }

    if "salut tout le monde" == 1 {
        Output > "one == one"
        Output > "endf"
    }
}